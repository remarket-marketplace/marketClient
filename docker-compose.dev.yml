services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: force-market-frontend-dev
    ports:
      - "3333:3333"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
